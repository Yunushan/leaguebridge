# v0.1.0 live release assessment — 2026-09-23

At **2026-09-23T17:20:55.5086081Z**, an authenticated invocation of
[`readiness verify-release`](../RELEASE_ASSESSMENT.md) for the published
[`v0.1.0` release](https://github.com/Yunushan/leaguebridge/releases/tag/v0.1.0)
completed with exit code 0 and `ok: true`. It derived **83/100**: a 74-point
released repository baseline plus nine points from live CI and publication
evidence. The released scorecard expires at **2026-10-06T00:00:00Z**.

| Verified identity | Value |
| --- | --- |
| Release ID | `384256185` |
| Release run | [`34150052066`](https://github.com/Yunushan/leaguebridge/actions/runs/34150052066), attempt 1 |
| Main-branch CI run | [`34147122944`](https://github.com/Yunushan/leaguebridge/actions/runs/34147122944), attempt 1 |
| Source commit | `a2202eb7072cdfef5e62c179d341a3d79d4d91ce` |
| Source tree | `313fb6ff2a1ce314441f23720ff8efdbf933ffc1` |

| Live criterion | Points |
| --- | ---: |
| Linux race and vet | 3 |
| Complete nine-target Linux/BSD cross-build | 3 |
| Nine published Linux/BSD release archives | 2 |
| Release publication and attestation | 1 |

The assessment used the release's nine archives and `checksums.txt`, plus the
matching ten CI attestation artifacts and nine cross-build binaries. All nine
archive hashes matched the published checksum file; the released-source
`tools/releasecheck` passed for the complete archive set. The ten CI artifact
ZIP digests matched the [CI run's artifact inventory](https://github.com/Yunushan/leaguebridge/actions/runs/34147122944),
and GitHub/Sigstore verification passed for all ten published release files
with the Release workflow, tag, and hosted-runner constraints. The live
assessor then authenticated the released source, matching CI and Release
attempts, signed evidence, publication state, and final-state stability before
returning the score. Invocation syntax and the fixed evidence contract are in
[the release-assessment guide](../RELEASE_ASSESSMENT.md).

At **2026-09-23T18:12:22.8867152Z**, the new
`readiness verify-production --version v0.1.0` command completed with exit
code 0 and `ok: true` against those same downloaded release and CI files. It
authenticated the same release commit, tree, CI and Release runs, all ten
published assets, and released scorecard SHA-256
`bc8294b9ee974e76305916ceb72d5655eb6b148e828dd9c7f8c42a2711069945`.
It checked the six fixed external rows in that released scorecard and then
repeated the live release verification before output. Its score was **83/100**:
repository 74, release 83, and all six external rows explicitly reported as
`missing_evidence` with zero points. The output's earliest evidence expiry was
the released scorecard's **2026-10-06T00:00:00Z** expiry. This does not imply
that mutable publication state is guaranteed until then. As with
`verify-release`, the observation assumes the selected GitHub CLI and its
HTTPS trust store are trusted; this command does not attest that executable.

At **2026-09-23T18:25Z**, the current worktree's native staging tools accepted
the downloaded `leaguebridge_0.1.0_linux_arm64.tar.gz` release archive and
produced separate Debian `arm64` and RPM `aarch64` staging trees. Both passed
`tools/nativepackagecheck`; each manifest recorded the published archive digest
`11ac70ae9218c5b739aff550dc7a55383ed365577dd81089f478feec1f4632de`.
This was staging on Windows, not an arm64 package build, package-manager
installation, signature, publication, or physical native run. It earns no
external points.

At **2026-09-23T19:20:45.0932705Z**, after restoring the local GitHub CLI
session, a fresh `readiness verify-production --version v0.1.0` run completed
with exit code 0. It again derived **83/100** and reported each of the six
external rows at zero. The earliest evidence expiry remained
**2026-10-06T00:00:00Z**. A separate live package-candidate command also
completed against the published Linux arm64 archive and the verified Debian
staging tree using explicitly synthetic, unsigned package bytes. Its temporary
test package and candidate files were removed afterward. That score-free
integration check supplied no native package, signing, publication, or
installation evidence.

This result is an observation at the time above, **not** a lasting
certification or a score for the current uncommitted source tree. The local
changes made after `v0.1.0` have not been published as a new release. The
remaining **17 points** require separate external proof: Riot authorization,
validated native integration, physical BSD/hardware results, independent audit
closure, signed and published native packages, and production lifecycle/native
smoke results. The assessment does not change the blocked local Linux/BSD
gameplay gate or unvalidated physical remote routes. A later claim needs a
fresh live assessment of its named release.
